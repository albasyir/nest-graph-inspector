---
title: Introduction
description: Nest Graph Inspector generates runtime dependency graphs from your NestJS application.
navigation:
  icon: i-lucide-house
---

**Nest Graph Inspector** is a NestJS module that generates a **runtime dependency graph** in **Markdown + Mermaid** or **JSON** format from the Nest application container.

The generated graph shows:

- Loaded modules from the root module
- Import relationships between modules
- Providers and controllers in each module
- Dependencies between providers/controllers
- Internal dependencies, external module dependencies, and selected NestJS core dependencies

::callout{icon="i-lucide-info" color="primary"}
The graph is generated from the **runtime Nest container**, not from static source parsing. What you see is what's actually running.
::

## What You Get

When you add Nest Graph Inspector to your project, it produces a complete map of your application architecture:

::card-group
  :::card
  ---
  icon: i-lucide-network
  title: Module Graph
  ---
  See every module and how they import each other.
  :::

  :::card
  ---
  icon: i-lucide-boxes
  title: Provider Dependencies
  ---
  Trace which providers depend on which, across module boundaries.
  :::

  :::card
  ---
  icon: i-lucide-route
  title: Controller Dependencies
  ---
  Understand what each controller needs to function.
  :::

  :::card
  ---
  icon: i-lucide-file-text
  title: Mermaid Diagrams
  ---
  View interactive dependency graphs rendered as Mermaid diagrams directly in GitHub or VSCode.
  :::
::

## Version Support

Official support: **NestJS 10-11**.

::callout{icon="i-lucide-lightbulb" color="neutral"}
Earlier versions may still work but are not officially supported. You can force install with `npm install nest-graph-inspector --force`. If it works for you, [raise an issue](https://github.com/albasyir/nest-graph-inspector/issues) so support coverage can be updated.
::
