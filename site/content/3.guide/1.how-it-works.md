---
title: How It Works
description: Understand the internal flow of Nest Graph Inspector.
navigation:
  icon: i-lucide-workflow
---

Nest Graph Inspector works by introspecting the **runtime Nest container** after your application bootstraps. This means the graph reflects what is actually loaded and wired — not what your source code suggests.

## Flow

::steps
### Start from the root module
The configured `rootModule` is the entry point. This is typically the same module you pass to `NestFactory.create()`.

### Inspect the Nest runtime container
The module uses NestJS internal APIs to access the dependency injection container after it has been initialized.

### Collect module and provider metadata
For each module, the inspector gathers:
- Imported modules
- Exported providers
- Registered providers
- Registered controllers

### Resolve dependencies
For each provider and controller, the inspector resolves the constructor dependencies, tracing them back to their originating module.

### Generate output
The collected data is formatted as Markdown + Mermaid diagrams or JSON and written to the configured output file.
::
