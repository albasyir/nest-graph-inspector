# Keep the workspace honest: a package may only import what it declares in its
# own package.json. Hoisting would let lib/ resolve dependencies it never
# declared, which then breaks for consumers installing it from npm.
shamefully-hoist=false
strict-peer-dependencies=false
